#spring.application.name=ms-lending-api
#spring.cloud.kubernetes.enabled=false
#spring.cloud.vault.enabled=false

spring:
  application:
    name: ms-lending-api
  cloud:
    kubernetes:
      config:
        enabled: false
      enabled: false
